import { createTheme } from "@mui/material";

export const regularTheme = createTheme({
  palette: {
    primary: {
      main: "#800080",
    },
    secondary: {
      main: "#e0d8e3",
    },
  },
});
